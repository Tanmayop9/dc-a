# .env is NOT used at runtime — all options are entered interactively in the console.
# This file is kept only as a reference for the available options.
#
# USER_TOKEN        — your Discord user token
# SOURCE_SERVER_ID  — ID of the server to clone from
# TARGET_SERVER_ID  — ID of the server to clone into
# SKIP_CHANNEL_IDS  — comma-separated channel IDs to skip
# MSG_LIMIT         — max messages to clone per channel (default 100)
# RATE_MS           — delay between API requests in ms (default 600)
# CONCURRENCY       — parallel channels for message cloning (default 3)
